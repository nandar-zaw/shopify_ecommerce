<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_shopify_product_sync_wizard_form" model="ir.ui.view">
            <field name="name">shopify.product.sync.wizard.form</field>
            <field name="model">shopify.product.sync.wizard</field>
            <field name="arch" type="xml">
                <form string="Sync Products">
                    <group>
                        <field name="operation"/>
                    </group>
                    <group string="Progress" invisible="total_records == 0">
                        <field name="total_records" readonly="1"/>
                        <field name="processed_records" readonly="1"/>
                        <field name="failed_records" readonly="1"/>
                        <field name="progress_pct" readonly="1"/>
                    </group>
                    <group>
                        <field name="result_message" readonly="1" nolabel="1"/>
                    </group>
                    <footer>
                        <button name="action_run" type="object" string="Run Sync" class="btn-primary"/>
                        <button string="Close" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_shopify_product_sync_wizard" model="ir.actions.act_window">
            <field name="name">Sync Products</field>
            <field name="res_model">shopify.product.sync.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>

